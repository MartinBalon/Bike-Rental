import styled from 'styled-components';
import { fontSize, inputStyle } from '../../../../common/theme';

export const StyledInput = styled.input`
	${inputStyle}
`;

export const StyledLabel = styled.label`
	font-size: ${fontSize.small};
`;
